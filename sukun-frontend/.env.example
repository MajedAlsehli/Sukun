# true: use the built-in presentation fixtures
# false: connect only to the configured backend
NEXT_PUBLIC_DEMO_MODE=false

# Accepted with or without a trailing /api suffix.
# Never place secrets in NEXT_PUBLIC_* variables.
NEXT_PUBLIC_API_URL=http://localhost:4000

